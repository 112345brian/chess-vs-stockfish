Fixed a hang (~10 second timeout, then "Could not verify that move") whenever a game, drill, or calibration puzzle ended in checkmate — asking the engine to search a position with no legal moves caused it to never respond. Positions with no legal moves are now recognized and scored directly without asking the engine at all, so a mate-delivering move grades correctly (and instantly) instead of timing out.
