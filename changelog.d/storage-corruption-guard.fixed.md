Fixed the page failing to load entirely if its saved rating or game history in localStorage was corrupted or from an incompatible older version. Stored data is now validated on load and discarded in favor of sane defaults rather than throwing during boot. Saving history also now degrades gracefully (dropping older games) instead of silently failing outright when browser storage is full.
